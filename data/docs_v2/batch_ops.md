# 배치 운영 가이드

## 기본 원칙
- 배치는 idempotent(재실행 가능)하게 만든다
- 재시도 정책(지수 백오프)과 데드레터 큐(DLQ) 고려

## 장애 시 대응
- 실패 지점 확인(입력 데이터/외부 의존성/DB 락)
- 부분 성공/중복 처리 여부 점검
- 필요 시 특정 구간만 재처리(리런)

## 체크리스트
- 스케줄러(cron) 정상 동작
- 배치 실행 시간 급증 여부
- 실패율/재시도 횟수 모니터링
