data_dir: "data/docs_v2"
index_dir: "index"

loader:
  allowed_exts: [".txt", ".md"]

chunk:
  size: 800         # 문자 기준
  overlap: 150

retrieval:
  top_k: 5

embedder:
  type: "sbert"
  sbert:
    model_name: "intfloat/multilingual-e5-small"